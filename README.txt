Abir Abbas
V4 Final

Compile :

make - compiles the shared library to the root directory!

To Run :

npm install
npm run dev port

Important:

All database functionality is near the bottom of the page. please make sure to login to enable/unlock all features.

if page is refreshed logging in again is required!

Most of the program is defensively programmed, The 5 Queries is under Database query panel, and the ad-hoc mode custom query typed in
by the user is in the custom query panel!
